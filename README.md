# Disco Playground

A tiny browser-based app playground for RC experiments.

Run locally:

```sh
npm run dev
```

Open `http://localhost:8080`.

Each saved app gets a path like:

```txt
/p/hello-rc/
```

The app stores playground files as JSON in `PLAYGROUND_DATA_DIR`.
